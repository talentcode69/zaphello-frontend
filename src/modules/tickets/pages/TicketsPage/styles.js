import styled from 'styled-components'

export const StyledTicketsPage = styled.div`
  display: flex;

  aside {
    flex-grow: 0;
    flex-shrink: 0;
  }

  main {
    flex-grow: 1;
    flex-shrink: 1;
  }
`
